{
  "name": "herramientas_1_pec2",
  "version": "1.0.0",
  "description": "Proyecto PEC2 - Herramientas HTML y CSS",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "parcel:dev": "parcel src/*.html -p 8123 --open",
    "parcel:build": "parcel build src/*.html --no-source-maps --no-cache",
    "clean": "rimraf dist .parcel-cache",
    "start": "npm-run-all clean parcel:dev",
    "build": "npm-run-all clean parcel:build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Francisco Javier Pérez Arribas <fperezarr@uoc.edu>",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel": "^2.8.1",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.2.1"
  }
}
